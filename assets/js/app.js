Promise.all([loadJson("data/config.json"),loadJson("data/casts.json"),loadJson("data/schedules.json")]).then(([c,d,s])=>{document.getElementById("cast-list").innerHTML=d.casts.map(x=>{const m=smap(s,x.id),dots=nextDays(c).slice(0,14).map(day=>`<i class="mini-day ${m.has(ymd(day))?"available":"unavailable"}"></i>`).join("");return `<a class="cast-card" href="cast.html?id=${x.id}"><div class="cast-thumb-wrap"><img src="${x.coverImage}" alt="${x.name}">${recommended(c,x.id)?'<span class="recommend-badge">店長おすすめ</span>':''}<span class="photo-count">写真 ${x.images.length}枚</span></div><div class="cast-body"><h3>${x.name}</h3><p>${x.catch}</p><div class="tag-row">${x.skills.map(v=>`<span class="tag">${v}</span>`).join("")}</div><p>${x.summary}</p><div class="mini-schedule">${dots}</div></div></a>`}).join("")}).catch(console.error)